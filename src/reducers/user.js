import {} from 'src/actions/types.js';

const initialState = {
  connected: false

}

const shop = (state = initialState, action) => {
  switch(action.type) {
    case 0 :
      return {
        ...state
      }
    default :
      return {
        ...state
      }
  }
    
  
}

export default shop;
