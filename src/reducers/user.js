import {REDIRECT} from 'src/actions/types.js';



const initialState = {
  connected: false

}

const shop = (state = initialState, action) => {
  
  switch(action.type) {
    
    default :
      return {
        ...state
      }
  }
    
  
}

export default shop;
